This is a repository to store the standalone components which will be used by the website agents to load the components dynamically.
The components need to use the react and tailwindcss.
The styling need to be consistent across the components.
The componets need to be responsive for the differnt viewports and the containers in which they will be loaded. 